//use std::collections::HashMap;

#[derive(Debug)]
pub enum JValue {
  //Str(String),
  //Num(Num),
  //Obj(HashMap<String, JValue>),
  //Array(Vec<JValue>),
  //Bool(bool),
  Null,
}

// #[derive(Debug)]
// pub enum Num {
//   Int(i32),
//   Float(f32),
// }
